import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.example.gmportfolio",
  appName: "GmPortfolio",
  webDir: "public",
  server: {
    url: "https://portfolio.gunjanmishra.com/",
  },
};

export default config;
